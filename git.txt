git add xxx 添加文件到暂存区
git commit -m "提交的注释"
git commit -m 一次性提交所有文件
把刚刚添加到缓存去的文件提交到仓库
git status 检查是否还有文件未提交
git diff xxx 查看提交文件有什么修改
cat xxx查看文件内容

版本回退：
1，git log 查看历史记录
git log -pretty=online 更直观看见版本
2,根据版本号回退
第一种方法是git reset --hard HEAD^回到上次
第二种方法是git reset 0--hard HEAD~前多少次的版本
3,恢复最新版本
git reflog 获取所有提交过的版本号
git reset --hard 版本号
通过此命令恢复版本
git checkout --xxxx撤销未提交前的命令
rm xxx删除文件
116.18.23.196


